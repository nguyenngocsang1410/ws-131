# Template — copy to .env and fill in. Safe to commit (no real secret here).
#   cp .env.example .env   &&   $EDITOR .env
#
# Load before running ansible:
#   set -a; source .env; set +a
#
BECOME_PASSWORD=your-sudo-password-here
